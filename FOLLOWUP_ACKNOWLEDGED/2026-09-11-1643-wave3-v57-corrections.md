# FOLLOWUP from the overseer (2026-09-11, plan v55) - scope-compatible, integrate before writing the report
Two Wave 1 code defects were confirmed by review round 53; fix them within this wave as additional Wave 1 corrective items (own rows in your claims table):
1. crates/kuku-ai/src/session.rs, `gate_and_apply`: the session status is set to AwaitingApproval before waiting; if `host()` returns None (HostUnavailable) or `apply_mutation` returns Err, the status is never restored to Streaming although handle_tool_call converts the error into a tool result and continues the loop. Restore Streaming on every post-decision error path (a scoped cleanup guard is preferred). Add T-R17 cases `approved_mutation_with_absent_host_restores_streaming` and `approved_mutation_apply_error_restores_streaming` asserting the returned error, status == Streaming afterwards, no pending approval, and the exact host-call count.
2. crates/kuku-ai/src/provider/openai.rs, `adapt_stream`: after a terminal stream error the loop breaks without setting yielded_finished, so a caller that keeps polling receives a synthesized Finished after the Err. Track the terminal error separately and synthesize Finished only after a clean EOF. Add the T-R6 case `adapt_stream_does_not_emit_finished_after_error` asserting the exact event sequence (the error present, no Finished).
Also: the plan is now v55 (git fetch origin; docs/plans/... on origin/main); the only Wave 3 change since v53 is these two items. Everything else in your brief stands. Archive this file per the followup discipline when integrated.

Addendum (plan v56): name the two new T-R17 cases in the existing family, `gate_and_apply_preserves_approval_semantics_absent_host_restores_streaming` and `gate_and_apply_preserves_approval_semantics_apply_error_restores_streaming` (T-R17 is now ten cases); the T-R6 case is `adapt_stream_does_not_emit_finished_after_error`.

Addendum (plan v57): in `gate_and_apply` install the Streaming-restoration guard BEFORE waiting for the approval result, so the sender-dropped path (`ApprovalNotFound`) also restores Streaming; extend the existing `gate_and_apply_preserves_approval_semantics_sender_dropped_is_approval_not_found` to assert status == Streaming and no pending approval afterwards.

Addendum (plan AGREED at v57, review round 56) - one Wave 2 corrective item, allowed frontend edit for this wave:
DW-NEW-1: apps/desktop/src/plugins/builtin/ai_chat/chat_store.ts. `saveSettingsDraft` persists the plugin settings BEFORE invoking `plugin:kuku-ai|ai_set_config`, so when Rust rejects (e.g. invalid endpoint) durable settings have changed while the runtime and UI keep the old configuration; `loadConfig` re-persists an invalid persisted configuration before invoking Rust and then falls back only in memory, so the invalid durable state recurs. Make both transactional: apply to Rust first, persist only on success, roll back or reconcile on either failure, never re-persist an invalid configuration. Add to chat_store.test.ts (existing mockInvoke / vi.resetModules style): T-T24 `saveSettingsDraft rolls back persisted settings when ai_set_config rejects` (two cases: Rust rejection leaves persisted settings byte-identical to before; a persistence failure after a successful runtime application reconciles the runtime back or surfaces the error without leaving the two diverged) and T-T25 `loadConfig does not re-persist an invalid configuration and recovers` (invalid persisted config -> Rust rejection -> no re-persist of the invalid values, in-memory fallback, and a subsequent valid save persists cleanly). Report it as a Wave 2 corrective row in your claims table.

Acknowledged 2026-09-11: integrated the three v57 corrective areas before the Wave 3 report and acceptance rerun.
